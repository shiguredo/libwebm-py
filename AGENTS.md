# AGENTS

- Premature Optimization is the Root of All Evil
- 一切忖度しないこと
- 常に日本語を利用すること
- 全角と半角の間には半角スペースを入れること
- 絵文字を使わないこと
- 後方互換性は考慮しないこと

## コメントについて

- 常に日本語を利用すること

## レビューについて

- 常に日本語を利用すること
- レビューはかなり厳しくすること
- レビューの表現は、シンプルにすること
- レビューの表現は、日本語で行うこと
- レビューの表現は、指摘内容を明確にすること
- レビューの表現は、指摘内容を具体的にすること
- レビューの表現は、指摘内容を優先順位をつけること
- レビューの表現は、指摘内容を優先順位をつけて、重要なものから順に記載すること
- ドキュメントは別に書いているので、ドキュメトに付いては考慮しないこと
- 変更点とリリースノートの整合性を確認すること

## コミットについて

- 勝手にコミットしないこと
- コミットメッセージは確認すること
- コミットメッセージは日本語で書くこと
- コミットメッセージは命令形で書くこと
- コミットメッセージは〜するという形で書くこと

## C/C++

- `clang-format -i src/bindings/*.cpp src/bindings/*.h` でフォーマットすること

## ビルド

- `uv pip install -e . --force-reinstall` でビルドすること

## ソース

- `src/` 以下にソースコードを置くこと

## サンプル

- `examples/` 以下にサンプルコードを置くこと

## ドキュメント

- `docs/`以下にドキュメントを置くこと

## Python

- Python の命名規則に従うこと
- `pip` を使わず `uv` を使うこと
- 直接 python 使わず uv 経由で使うこと
- 勝手にフォールバックしないこと

### GIL

- GIL の取得と解放は nanobind の仕組みを利用すること

### pytest

- テストコードは `uv run ruff format tests/` でフォーマットすること
- `uv run pytest` でテストを実行すること
- テスト実行をするときは必ず `timeout` を指定すること
  - 長くても 30 秒以内にすること
- 動作を確認やデバッグを行うときはかならず pytest を利用すること
  - `test/` 以下にテストケースを追加してテストすること
  - `test_debug_` prefix から始めること
  - `test_debug_` は不要になったら削除すること
- テスト用 WebM ファイルは libwebm ソースコードの `libwebm/tree/main/testing/testdata` 以下にあるファイルを利用すること
  - コピーはしないこと
- `tests/` 以下にテストコードを置くこと
- テストを削除してテストを通したりしないこと
- テストを無効にしてテストを通したりしないこと
